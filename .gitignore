node_modules/
dist/
.vercel/
harness/.browsers/

# Raw per-frame probe dumps and DevTools traces are large (1.5-12 MB each) and
# regenerable with `npm run measure`. The distilled analyses, run logs and the
# confirmation videos are committed under evidence/ instead.
results/
*.log
